# Core Flask framework
Flask>=2.3.0

# Database ORM for safe SQL queries (prevents SQL injection)
Flask-SQLAlchemy>=3.0.0
SQLAlchemy>=2.0.0

# Environment variables management
python-dotenv>=1.0.0

# WSGI utility library (included with Flask but specified for clarity)
Werkzeug>=2.3.0

# Template engine (included with Flask)
Jinja2>=3.1.0

# Development server
flask-cors>=4.0.0

# Optional: For production deployment
gunicorn>=21.0.0

# Optional: For database migrations (if using Flask-Migrate)
# Flask-Migrate>=4.0.0

# Optional: For form handling and validation
# Flask-WTF>=1.1.0
# WTForms>=3.0.0

# Optional: For email functionality
# Flask-Mail>=0.9.1

# Optional: For API documentation
# Flask-RESTX>=1.1.0

# Optional: For caching
# Flask-Caching>=2.0.0

# Testing dependencies (for development)
pytest>=7.0.0
pytest-flask>=1.2.0


#------------------------------------------------|
#Moduł wiadomości i sportu:
# Zmienne środowiskowe
python-dotenv
#scraping
beautifulsoup4
# Parser HTML dla BeautifulSoup
lxml
# Strefa czasowa
pytz
#------------------------------------------------|